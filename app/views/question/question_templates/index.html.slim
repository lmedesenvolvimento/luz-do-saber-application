/ - model_class = Question::QuestionTemplate


/ .page-header
/   h1=t '.title', :default => model_class.model_name.human.pluralize.titleize
/ table.table.table-striped
/   thead
/     tr
/       th= model_class.human_attribute_name(:type)
/       th= model_class.human_attribute_name(:name)
/       th=t '.actions', :default => t("helpers.actions")
/   tbody
/     - @question_templates.each do |question_template|
/       tr
/         td= link_to question_template.id, question_template_path(question_template)
/         td= question_template.type
/         td= question_template.name
/         td=l question_template.created_at
/         td
/           = link_to t('.show', :default => t("helpers.links.show")), question_template_path(question_template), :class => 'btn btn-default btn-xs'
/           '
/           / = link_to t('.edit', :default => t("helpers.links.edit")), edit_question_question_template_path(question_template), :class => 'btn btn-default btn-xs'
/           / '
/           / = link_to t('.destroy', :default => t("helpers.links.destroy")), question_question_template_path(question_template), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs btn-danger'

/ = link_to t('.new', :default => t("helpers.links.new")), new_question_template_path, :class => 'btn btn-primary'

#app_vue

== javascript_pack_tag 'application'