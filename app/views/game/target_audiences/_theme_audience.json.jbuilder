json.extract! theme_audience, :id, :title, :slug, :cover_full_url, :status, :themes, :order

# json.themes do
#   json.array! @themes do |theme|
#     json.extract! theme, :id, :title, :status, :slug
#   end
# end
